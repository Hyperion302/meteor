{
    "name": "meteor",
    "version": "1.0.0",
    "description": "Meteor web app",
    "author": "Joseph Shetaye",
    "private": true,
    "scripts": {
        "dev": "export GOOGLE_APPLICATION_CREDENTIALS=\"key.json\" && nuxt",
        "build": "export GOOGLE_APPLICATION_CREDENTIALS=\"key.json\" && nuxt build",
        "start": "export GOOGLE_APPLICATION_CREDENTIALS=\"key.json\" && nuxt start",
        "generate": "export GOOGLE_APPLICATION_CREDENTIALS=\"key.json\" && nuxt generate",
        "lint": "eslint --ext .js,.vue --ignore-path .gitignore .",
        "lintFix": "eslint --fix --ext .js,.vue --ignore-path .gitignore ."
    },
    "dependencies": {
        "@nuxtjs/axios": "^5.3.6",
        "firebase": "^7.7.0",
        "firebase-admin": "^8.9.1",
        "nuxt": "^2.0.0"
    },
    "devDependencies": {
        "@nuxt/typescript-build": "^0.5.6",
        "@nuxtjs/eslint-config": "^1.0.1",
        "@nuxtjs/eslint-module": "^1.0.0",
        "babel-eslint": "^10.0.1",
        "eslint": "^6.8.0",
        "eslint-config-prettier": "^4.3.0",
        "eslint-plugin-nuxt": ">=0.4.2",
        "eslint-plugin-prettier": "^3.1.2",
        "prettier": "^1.19.1"
    }
}
