rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
      match /videos {
          match /{videoId} {
              allow read: if request.auth.uid != null;
          }
      }
  }
}